# OpenRouter retired in v19

Craft Connect v19 no longer uses OpenRouter for its AI Edge Functions.

Use Google Gemini instead. See `GEMINI_SETUP.md`.

The old `OPENROUTER_API_KEY` / `OPENROUTER_MODEL` secrets can remain temporarily, but they are not read by the v19 AI functions. Remove them after Gemini is verified.
